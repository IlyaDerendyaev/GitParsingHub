import requests

def user_projects(user):
	r = requests.get("https://api.github.com/users/" + user + "/repos")
	list = r.json()
	projects = []
	for element in list:
		if element['watchers'] > 2:
			projects.append(element['full_name'])
	return projects
